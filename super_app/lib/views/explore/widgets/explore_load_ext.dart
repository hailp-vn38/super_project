part of '../view/explore_view.dart';

class ExploreLoadExt extends StatelessWidget {
  const ExploreLoadExt({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
